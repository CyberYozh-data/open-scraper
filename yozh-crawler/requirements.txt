fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.6
pydantic-settings>=2.2
httpx[socks]>=0.26
lxml>=5.2
fastapi-mcp>=0.4.0,<0.5.0
# Same bound as the parent service: yozh-crawler/src/app.py makes the same
# FastApiMCP(app, ...) call, and `docker compose build` builds both — so
# without this the next rebuild swaps one broken service for another.
mcp>=1.28.1,<2
