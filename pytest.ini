[pytest]
pythonpath = .
testpaths = tests
# The CI gate selects on this string, so an unregistered typo would silently
# promote a heavyweight test into the fast job.
markers =
    e2e: needs a live browser, real servers or network; excluded from the fast gate
addopts = --strict-markers
