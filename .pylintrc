# NOTE: two services score against this file. The root `src/` tree is gated at
# 9.96 and yozh-crawler/src at 9.85 (the crawler job passes --rcfile explicitly,
# because pylint cannot discover this file from a non-package directory).
# Re-enabling a check here moves BOTH numbers — re-enabling the docstring
# checks, for one, drops the crawler from 9.90 to 9.01 and reds its job.
[MASTER]
# Use multiple processes
jobs=1

[MESSAGES CONTROL]
disable=
    # Docstrings — not required for this project
    missing-module-docstring,
    missing-class-docstring,
    missing-function-docstring,

    # Import errors — pylint doesn't see the venv packages (fastapi, playwright, etc.)
    import-error,

    # Design choices — too-many-* thresholds don't apply to worker/runner architecture
    too-many-arguments,
    too-many-positional-arguments,
    too-many-locals,
    too-many-statements,
    too-many-branches,
    too-many-instance-attributes,

    # _worker_main is a subprocess entry point — local imports are intentional
    import-outside-toplevel,
    reimported,

    # Internal API access within the same module group
    protected-access,

    # broad-exception-caught is intentional in worker/pump loops
    broad-exception-caught,

    # Duplicate code — schemas intentionally mirror each other
    duplicate-code,

    # Pydantic false positives — pylint can't introspect pydantic field access at runtime
    no-member,

    # too-few-public-methods — config/filter classes don't need 2+ methods
    too-few-public-methods,

    # unused-argument — FastAPI/Protocol interface signatures must match even when unused
    unused-argument,

    # redefined-outer-name — pytest fixtures are injected by name; pylint can't see that
    redefined-outer-name,

[FORMAT]
max-line-length=120

[DESIGN]
max-args=10
